# [ALL] Frozen dependency set — Python 3.11. Same versions for everyone so the
# golden case reproduces. After the first successful `pip install`, lock exact
# versions with `pip freeze > requirements.lock` and commit that too.
langgraph==0.2.39
langchain-core==0.3.15
langchain-ollama==0.2.0
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2
httpx==0.27.2
python-dotenv==1.0.1
pytest==8.3.3
